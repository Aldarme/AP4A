Projet AP4A ROCHU Gaspard

To run the programm :
cd directory
cmake
cmake --build .
Go in the debug file and run the .exe

The programm is a simulation for the indoor condition of a flying plane, where datas are randomly generated.
